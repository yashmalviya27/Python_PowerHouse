# encaptulation in python 
# it is the process of wrapping data and methods into a single unit called class

# this is the public class
""" class Student:
    def __init__(self, name, age, marks):
        self.name = name
        self.age = age
        self.marks = marks

    def display(self):
        print(f"Name: {self.name}\nAge: {self.age}\nMarks: {self.marks}")

s1 = Student("Yash", 20, 85)
s1.display() """

# this is the private class
class Student:
    def __init__(self, name, age, marks):
        self.__name = name
        self.__age = age
        self.__marks = marks

    def __display(self):
        print(f"Name: {self.__name}\nAge: {self.__age}\nMarks: {self.__marks}")

s1 = Student("Yash", 20, 85)
s1.__display()
# encaptulation in python 
# it is the process of wrapping data and methods into a single unit called class

# this is the public class
""" class Student:
    def __init__(self, name, age, marks):
        self.name = name
        self.age = age
        self.marks = marks

    def display(self):
        print(f"Name: {self.name}\nAge: {self.age}\nMarks: {self.marks}")

s1 = Student("Yash", 20, 85)
s1.display() """

# this is the private class
class Student:
    def __init__(self, name, age, marks):
        self.__name = name
        self.__age = age
        self.__marks = marks

    def __display(self):
        print(f"Name: {self.__name}\nAge: {self.__age}\nMarks: {self.__marks}")

s1 = Student("Yash", 20, 85)
s1.__display()
# encaptulation in python 
# it is the process of wrapping data and methods into a single unit called class

# this is the public class
""" class Student:
    def __init__(self, name, age, marks):
        self.name = name
        self.age = age
        self.marks = marks

    def display(self):
        print(f"Name: {self.name}\nAge: {self.age}\nMarks: {self.marks}")

s1 = Student("Yash", 20, 85)
s1.display() """

# this is the private class
class Student:
    def __init__(self, name, age, marks):
        self.__name = name
        self.__age = age
        self.__marks = marks

    def __display(self):
        print(f"Name: {self.__name}\nAge: {self.__age}\nMarks: {self.__marks}")

s1 = Student("Yash", 20, 85)
s1.__display()
